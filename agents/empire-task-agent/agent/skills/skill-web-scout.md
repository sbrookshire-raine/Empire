# skill-web-scout

When the Architect wants a public page captured for triage:

1. Ensure Toolbelt **Web Scout** is on.
2. Call `web_scout` with the http(s) URL.
3. Summarize from the returned `summary` / path — never invent page content.
4. Never call `cognee_remember` unless they explicitly ask to promote.
