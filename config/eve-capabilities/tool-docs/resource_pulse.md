---
name: resource_pulse
toolbelt: always
one_line: Resource pulse: RAM/disk/GPU lease, services, effective tools, which light skills are safe to admit now, and …
---

## Description (verbatim from the tool schema, pre-R-03)

Resource pulse: RAM/disk/GPU lease, services, effective tools, which light skills are safe to admit now, and which GPU/heavy skills need Architect OK. Call before admitting capabilities or starting heavy work. Never auto-remembers Cognee.

## Parameters

- (no parameters)

## Usage

Registered by the Toolbelt category `always`. The model sees the name, the
one-line cue and the parameter names in its schema; this document is the deep syntax it
can fetch with `tool_docs` when a call needs more than the cue.
