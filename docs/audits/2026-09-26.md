# EMPIRE audit — 2026-09-26

Phase 1 measurement record. Everything below was **measured**, not recalled; readings taken
2026-09-26 between 10:11 and 10:45 local, after a full stop → cold start. Where a number comes from
an earlier measurement the same day, it says so.

Scope: services, runtime placement, Eve's context budget, hands and coverage, data surfaces,
gates, documents, hygiene, known failure modes. Phase 2 (contradiction fixes) and Phase 3
(operating contract) act on this list.

## 1. Services and startup

| Reading | Value |
|---|---|
| Ports listening | **6/6** — 8080 Workbench, 2000 Eve, 8090 PocketBase, 11434 Ollama, 8000 Speaches, 8888 Docker/WSL relay |
| Health endpoints | **4/4 HTTP 200** — `/api/health`, `/api/memory/status`, `/eve/v1/info`, `/health` |
| Cold start (wall) | **25.4 s**, internal stopwatch **`Startup: 19.5s`** |
| Cold start when Docker Desktop must also boot | 42.1 s wall / 33.1 s internal |
| Cold start before the parallel-launch change | **75 s** |
| Core containers | `empire-searxng`, `empire-cognee-postgres` (healthy), `empire-speaches` |
| Sandbox containers | running **4** (exited ones reaped by `prune-sandbox-containers.ps1`) |
| Processes | python **2** / 412 MB · node **3** / 358 MB · ollama 48 MB · pocketbase 23 MB (~840 MB total) |
| Processes earlier the same day | python **16** / 862 MB (orphaned MCP twins, since reaped) |

**Threshold:** all six services healthy, nothing started twice, cold start ≤ 60 s. Met.

## 2. Runtime and placement

| Reading | Value |
|---|---|
| Window reported by **her own runtime** | `contextWindowTokens: 24576` (model `openai/empire-fast:14b`) |
| Endpoint naming caveat | her runtime labels routing/endpoint `provider: "openai"` — that is the **local OpenAI-compatible Ollama endpoint on 11434**, not a cloud service |
| VRAM while idle | **0** — no model resident between turns |
| VRAM loaded, documented tuning | **12.6 GB** (f16 KV, flash-attn off, `NUM_PARALLEL=1`, ctx 24576) |
| VRAM, ad-hoc tuned state earlier the same day | 10.5 GB (q8_0 KV + FA) — see contradiction C1 |

## 3. Eve's context budget (the scarcest resource)

| Reading | Value |
|---|---|
| Always-on routing file | `agent/empire-routing.md` — 96 lines / 9.2 KB |
| Skills on disk | 33 (= 33 declared — consistent) |
| Playbook limbs | 8 |
| Claimed tools | 85, coverage `ok: true` |
| `agent/eve_instructions.md` | **referenced by a code comment, does not exist.** The always-on prompt is assembled by `agent/instructions.ts` + `empire-routing.md` — see C3 |

Context discipline is good: the routing pair shares **0 lines** (compact file delegates to a loadable
skill), so nothing is duplicated into the always-on prompt.

## 4. Hands and coverage

| Reading | Value |
|---|---|
| Capability governance (gate) | verified **7**, fail **0** |
| Playbook coverage | `ok: true` — claimed 85, missing 0, ghost 0, unused 0 |
| Wrapper consolidation | 7 `agent/lib/*-mcp.ts`: **1,328 → 299 lines** + one 175-line `mcp-client.ts` |
| Wrapper runtime coverage | **nothing in `tests/` or `scripts/` calls any `*ViaMcp` wrapper** — only Eve's live turns exercise them. Build + gate prove wiring, not calls |
| `github_scout_search always defineTool` | smoke check **FAILs** (expects static `defineTool`; toolbelt registers dynamically per turn). Check dates to 2026-09-17 — see C2 |

## 5. Data surfaces

| Surface | Reading |
|---|---|
| `C:\Empire_Workbench` (vault; `read_document` relative-path root) | 14,651 files / 2.68 GB / 13,444 markdown |
| Vault ingest state | 612 batches, **11,644 ingested**, 594 recorded skipped, 0 pending |
| Skip log | 1,683 lines but **870 unique paths**; ~796 are whitespace-only or NUL-bearing junk with `.md` extensions (correctly refused); **66 files / 5.9 MB recovered** by the decoder fix, **not yet ingested** |
| `D:\wiki_md\2017` (corpus) | **5,347,264 files / 20.41 GB**, ~4 KB/file |
| Eve's wiki read path | lead ≤1800 chars + one section ≤1400 chars (~3 KB/article) |
| Weaviate | retiring by design — `weaviate_export.py` is a one-time read-only drain; the v2 archive is raw binary |
| Docker waste | images 33.75 GB (**9.54 GB reclaimable**), volumes 12.85 GB (**3.69 GB reclaimable**), containers 2.4 GB (1.71 GB reclaimable) — none pruned, needs sign-off |

## 6. Gates: what is covered, and what is not

Observed steps in `mechanic-green`: unit tests (**598 passed**), capability governance (fail-closed),
wiki extract battery, workbench UI harness (15 passed), `verify-stack`, `verify-eve-workbench`
(14 stages incl. live Cognee recall), prompt budget. `-Full` adds a live Eve workbench verify.

**Not covered by any gate:** wrapper runtime calls (§4); doc-vs-reality drift (the missing
`eve_instructions.md`); port collisions beyond the start scripts' own checks; vault ingest
completeness (the 66 recovered files are invisible to the gate).

## 7. Documents

756 markdown on disk / 441 tracked / 13,338 KB (was 754 before `DOC_MAP.md` and
`WIKI_LAYER_AUDIT.md` were added today). Generated trees (`.eve` snapshots, `.output`, uploads,
dashboard) are git-ignored and excluded from search. Vendor surface: `docs/reference` 47 files,
`tools/archify` 302 files.

## 8. Hygiene and attribution

| Reading | Value |
|---|---|
| Dead-module scan (stem-based, 34 wiki modules) | **1 unreferenced**: `pipeline/wiki_xml_convert.py` — the one-shot recipe for the 20 GB corpus. Keep. |
| Licence/SPDX headers repo-wide | **0 files.** The repo is public and multi-source (Gemini, Composer 2, Deepseek, others), so no piece states its origin or licence — see C5 |

## 9. Known failure modes (named, so they stop being mysteries)

- **E-30 — slow generation:** turn `1b9787f3524842b8bbd5e4c00cb6101b`, `stream.end` at 326.82 s; tools
  succeeded, then ~44 chunks of 1–10 chars (~0.7 chars/s). Not a hang; both events landed just past 300 s.
- **Empty replies** (e.g. `rb_04`) — no answer text at all; should fold into E-30's signature.
- **Ollama `server.log` never rotates** — stale `n_ctx_slot = 16384` lines misled a diagnosis today.
- **SearXNG keeps no request log** — "did a query arrive?" has no cheap answer.
- **Unscoped file enumeration** — a recursive listing of the wiki corpus exceeded 300 s; any inventory
  of that tree must be scoped or pre-indexed.

## 10. Contradictions to resolve in Phase 2

| ID | Contradiction | Decision needed |
|---|---|---|
| C1 | `docs/PLACEMENT.md` (q8_0 + FA = 11.3 GB) vs `ensure-ollama-parallel.ps1` (f16 + FA off = 12.6 GB) | which document is authoritative |
| C2 | smoke check expects static `github_scout_search`; toolbelt registers it dynamically | update the check, or force static (costs prompt budget) |
| C3 | code comment references `agent/eve_instructions.md`; the file does not exist | fix the comment, or add the file |
| C4 | Three "how to run/use" docs (`AGENTS.md`, `EMPIRE_GUIDE.md`, `EMPIRE_USAGE_GUIDE.md`) — overlap **unmeasured** | measure first (the method that cleared the routing pair) |
| C5 | Attribution absent, repo public and multi-source | annotate all vendored pieces, or only new ones |

## 11. Open work carried in from earlier sessions

1. **Live wrapper call not yet exercised** — tracing is on, so the next real turn that calls one is captured.
2. **66 recovered vault files** need `ingest_workbench.py --resume` to reach memory.
3. **Docker reclaim** (~15 GB) awaits sign-off.
4. **Wiki sidecar index** and the four-path title resolution — designed, ranked in `WIKI_LAYER_AUDIT.md`.

