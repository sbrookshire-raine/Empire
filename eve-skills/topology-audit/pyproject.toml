[build-system]
requires = []
build-backend = "build_backend"
backend-path = ["."]

[project]
name = "topology-audit"
version = "0.1.0"
description = "Analyze declared dependencies and outage propagation"
requires-python = ">=3.10"
license = {text = "MIT"}
dependencies = []

[project.scripts]
topology-audit = "local_topology_audit.runtime:main"
