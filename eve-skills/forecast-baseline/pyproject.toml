[build-system]
requires = []
build-backend = "build_backend"
backend-path = ["."]

[project]
name = "forecast-baseline"
version = "0.1.0"
description = "Produce transparent univariate forecast baselines"
requires-python = ">=3.10"
license = {text = "MIT"}
dependencies = []

[project.scripts]
forecast-baseline = "local_forecast_baseline.runtime:main"
