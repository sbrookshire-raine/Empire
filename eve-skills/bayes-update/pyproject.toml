[build-system]
requires = []
build-backend = "build_backend"
backend-path = ["."]

[project]
name = "bayes-update"
version = "0.1.0"
description = "Update finite hypotheses from supplied priors and likelihoods"
requires-python = ">=3.10"
license = {text = "MIT"}
dependencies = []

[project.scripts]
bayes-update = "local_bayes_update.runtime:main"
