[build-system]
requires = []
build-backend = "build_backend"
backend-path = ["."]

[project]
name = "thought-map"
version = "0.1.0"
description = "Analyze a supplied signed directed graph"
requires-python = ">=3.10"
license = {text = "MIT"}
dependencies = []

[project.scripts]
thought-map = "local_thought_map.runtime:main"
